# 6-hour operation plan — Boring Receipts / TurboQuant

Date: 2026-05-23  
Orchestrator: Pi-main with Felipe  
Ledger: `/home/aya/implante/.pi-ensemble`

## Goal

Turn the current multi-session setup into a useful external validation surface without restarting the research loop.

The six-hour target is operational, not thesis-building:

1. keep the 3090 KV-dtype long-context job safe and watched;
2. make the public request surface clear (`REQUESTS.md`);
3. keep the 4090 lane healthy and default-off;
4. preserve design as a separate lane;
5. consolidate any completed 3090 output into one honest receipt or blocker;
6. stop with a clean handoff, not with a new speculative experiment.

## Lane assignments

| lane | owner/session | scope | hard boundary |
|---|---|---|---|
| Orchestrator | Pi-main | priority, narrative, claims, merge/push, public caveats | no social/external posting without Felipe |
| 3090 worker | `tmux: boring receipts lab` | AYA-3090 llama.cpp runtime receipts | no design, no RealRAG/4090 |
| 4090 worker | Pi-main for now | vLLM/RealRAG/EPKV health and future external receipts | no restart/patch/deploy without `CONFIRMAR:INFRA` |
| Design | `tmux: boring design` | `docs/`, `docs/assets`, CSS/HTML only | no metrics/receipts without handoff |

## Current baseline snapshot

- 3090: KV-dtype long-context source-build job is running under `boring receipts lab`.
- Watcher registered: `boring-kvdtype-longctx-3090` watching `q8_0-q4_0.stderr.txt` for `EXIT 0` / nonzero/failure markers.
- 4090: monitor OK; vLLM health endpoint responds; policy file is `{"enabled": false, "tag": "default-off"}`.
- Claims:
  - turboquant repo: `pi`
  - boring `receipts/`: `pi`
  - boring `docs/`: `claude`

## Timeline

### T+0:00–0:30 — Control tower

- [x] Register lane roles in `pi-ensemble`.
- [x] Set tmux metadata for `boring receipts lab` and `boring design`.
- [x] Start watcher for current 3090 long job.
- [x] Check 4090 monitor + vLLM default-off state.

### T+0:30–1:30 — Public request surface

- [x] Add `REQUESTS.md`: branch + command shape, accepted fields, hardware lanes, return format, no-claims.
- [ ] Link `REQUESTS.md` from README / CONTRIBUTING.
- [ ] Ask design lane to expose it in `docs/` when safe.

### T+1:30–3:30 — 3090 receipt integration if job completes

If `boring-kvdtype-longctx-3090` fires:

1. read `f16-f16.jsonl`, `q8_0-q8_0.jsonl`, `q8_0-q4_0.jsonl` from the remote output directory;
2. summarize context curve by KV dtype;
3. produce exactly one of:
   - a PASS/MIXED receipt if all commands completed and metrics are interpretable;
   - a BLOCKED receipt if the process hung, OOMed, or partial output cannot support a claim;
4. include the exact command and caveats;
5. update `NEGATIVES.md`/`SUMMARY.md` only if needed, not automatically.

No retest loop unless Felipe explicitly asks.

### T+3:30–5:00 — 4090 lane readiness, no new RealRAG experiment

- Keep service default-off.
- Prepare external-request intake for 4090, but do not run a new quality experiment without an external branch/command.
- If a real external command appears, classify it as:
  - local/docs only;
  - benchmark no service mutation;
  - service mutation requiring `CONFIRMAR:INFRA`.

### T+5:00–6:00 — Handoff and stop condition

- Commit/push only factual docs/receipts.
- Record ensemble note with:
  - 3090 job status;
  - watcher status;
  - files changed;
  - commits/pushes;
  - next safe action.
- Release claims that are no longer active.

## Stop rules

Stop immediately if:

- 4090 policy is not default-off after any check;
- vLLM health fails;
- 3090 job returns nonzero or stalls without new output;
- any lane tries to edit another lane’s path;
- a public/social post is proposed without Felipe’s explicit approval.

## Non-goals for this six-hour window

- No new EPKV/sampler experiments.
- No new hand-written verifier gates.
- No Discord/X outreach unless Felipe asks.
- No raw per-case dumps added to public repos.
